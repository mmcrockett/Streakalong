<% links = [:activities, :streaks, :settings, :logout] %>
      <div class="navbar navbar-default" id="nav-bar">
        <ul class="nav navbar-nav">
        <% links.each do |link| %>
          <li style="width:<%= (100 / links.size).floor %>%"><a href="/<%= link %>"><%= link %></a></li>
        <% end %>
        </ul>
      </div>
